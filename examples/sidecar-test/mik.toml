[project]
name = "sidecar-test"
version = "0.1.0"
type = "app"

[composition]
http_handler = true
# bridge auto-downloads from OCI registry if not specified

[server]
port = 3000
modules = "modules/"
